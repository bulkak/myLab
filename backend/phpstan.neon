# Уровень max — максимальная строгость PHPStan.
# После `php bin/console cache:warmup --env=dev` можно раскомментировать блок symfony
# для подсказок по сервисам из DI-контейнера (иначе файл контейнера может отсутствовать).

includes:
    - vendor/phpstan/phpstan-symfony/extension.neon
    - vendor/phpstan/phpstan-symfony/rules.neon
    - vendor/phpstan/phpstan-doctrine/extension.neon
    - vendor/phpstan/phpstan-doctrine/rules.neon

parameters:
    # Level 6: Report missing type hints on constants, methods, and properties
    # Level 7+ requires fixing strict type mismatches (37 errors found)
    level: 6
    paths:
        - src
    ignoreErrors:
        # Doctrine entities have nullable PHP properties for new objects, but non-null DB columns
        - identifier: doctrine.columnType
          message: '#type mapping mismatch#'
        - identifier: doctrine.associationType
          message: '#type mapping mismatch#'

#    symfony:
#        containerXmlPath: var/cache/dev/App_KernelDevDebugContainer.xml
